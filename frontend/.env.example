# Backend API URL — set this to your deployed backend URL
# For local dev, Vite proxy handles /api so this can be left empty
# For production (Vercel), set to your backend URL e.g. https://your-backend.railway.app/api
VITE_API_URL=/api
